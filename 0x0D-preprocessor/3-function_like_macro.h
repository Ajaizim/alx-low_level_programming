#ifndef MACROLIKE_H
#define MACROLIKE_H

/**
 * File: 3-function_like_macro
 *
 * @x: input value (an int)
 * Desc: header file that computes absolute value
 */

#define ABS(x) ((k) < 0 ? -(1) * (*) : (*))

#endif
